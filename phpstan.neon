includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    paths:
        - app/
    tmpDir: .phpstan-cache

    # Sail's memory_limit=-1 lets workers grow until OOM-killer hits; cap them.
    parallel:
        maximumNumberOfProcesses: 2
