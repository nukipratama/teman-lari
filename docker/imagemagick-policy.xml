<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE policymap [
<!ELEMENT policymap (policy)*>
<!ATTLIST policymap xmlns CDATA #FIXED "">
<!ELEMENT policy EMPTY>
<!ATTLIST policy xmlns CDATA #FIXED "">
<!ATTLIST policy domain NMTOKEN #REQUIRED>
<!ATTLIST policy name NMTOKEN #IMPLIED>
<!ATTLIST policy pattern CDATA #IMPLIED>
<!ATTLIST policy rights NMTOKEN #IMPLIED>
<!ATTLIST policy stealth NMTOKEN #IMPLIED>
<!ATTLIST policy value CDATA #IMPLIED>
]>
<!--
  Hardened over the stock "open" alpine policy. The only ImageMagick use in this
  app is the run-card rasteriser (app/Services/Run/Story/RunCardImageRenderer.php):
  it reads a server-built, fully-escaped SVG blob and writes a PNG. Nothing should
  ever fetch a URL, run a delegate over the network, or read a file indirectly — so
  those coders/paths are denied here as defence-in-depth against a future change
  that lets unescaped content reach librsvg. SVG/MSVG/PNG coders stay enabled (not
  listed = allowed) so rasterisation keeps working.
-->
<policymap>
  <policy domain="Undefined" rights="none"/>

  <!-- Network + scripting coders: never used, prime SSRF / file-read vectors. -->
  <policy domain="coder" rights="none" pattern="URL"/>
  <policy domain="coder" rights="none" pattern="HTTPS"/>
  <policy domain="coder" rights="none" pattern="HTTP"/>
  <policy domain="coder" rights="none" pattern="FTP"/>
  <policy domain="coder" rights="none" pattern="MSL"/>
  <policy domain="coder" rights="none" pattern="EPHEMERAL"/>
  <policy domain="coder" rights="none" pattern="TEXT"/>
  <policy domain="coder" rights="none" pattern="LABEL"/>
  <policy domain="coder" rights="none" pattern="{PS,PS2,PS3,EPS,PDF,XPS}"/>

  <!-- No indirect (@file) reads, and no delegate reaching out over the network. -->
  <policy domain="path" rights="none" pattern="@*"/>
  <policy domain="delegate" rights="none" pattern="URL"/>
  <policy domain="delegate" rights="none" pattern="HTTPS"/>
  <policy domain="delegate" rights="none" pattern="HTTP"/>
</policymap>
